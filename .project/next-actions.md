# Next Actions

## Priority

1. **(done) Centralization milestone committed** as `f9714dd Hard Reboot` on `main`. The final #8 typeshare-resolution tweaks (`models.ts`, `generate-types.sh`, `.gitignore`) + this `.project/` tracker were committed alongside it. Tree green (Rust 20 suites; Swift `NodMac`/`NodIOS` + `swift test` 24; desktop `tsc` + 16 vitest). Nothing left to publish locally — **push only when explicitly asked.**
   - `client/nod-desktop/src/dto/generated.ts` is **git-ignored** (regenerated by `scripts/generate-types.sh`) — not committed, by design.

2. **Re-run the green gate before committing:** `cargo test --workspace`; `cd client/nod-apple && swift test` + `xcodebuild` NodMac/NodIOS; `cd client/nod-desktop && npm run typecheck && npm test`.

3. **(done) #8 resolved** — full literal TS adoption evaluated and **reverted** (typeshare's optional-everything made the UI noisier). Final: Rust `#[typeshare]` contract + git-ignored drift-diff + hand-written client types + the desktop `source→channel` bug fix. No further action.

## Possible / alternate approaches

- (decided) Kept the hand-written `models.ts` mirror + a typeshare drift-diff; rejected literal adoption + `skip_serializing_if` (made the UI noisier). Revisit only if typeshare gains camelCase Swift + TS unions + `| null`.
- For Swift wire types: leave hand-written (current decision) unless a camelCase-Swift path appears.

## Later / parked

- Release-hosted xcframework `binaryTarget` (URL + checksum) when cutting the first release.
- watchOS xcframework slice when a watch target exists.
- CI drift-check comparing generated projections to consumers.
