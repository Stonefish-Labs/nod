# Session: 2026-06-09 12:41

## Conversation Context

- User intent: Continue the pre-publish "make the system make sense" refactor; then, at this point, **create the `.project/` tracker** via the `.codex/skills/project-tracker` skill while a parallel fork pushes #8 full adoption.
- Starting state: Continued from a prior session that landed #1–#7 (nod-proto extraction, crypto centralization, server + client rewire, frozen corpus, client digest recompute), #10 (source→channel, Rust side), #12 (Docker build). All uncommitted on `b6765f4 Rework`.
- Goal changes or pivots: #8 pivoted from "adopt typeshare everywhere" to "infra + fix the discovered desktop bug, defer literal adoption" → then user directed full adoption in a **parallel fork**.
- Current constraints: Burn the boats (no backcompat); security paramount; typeshare/UniFFI idioms clash with hand-written client idioms.

## Work Performed

- #11 relay: added `InProcessApnsProvider`, `RelayPolicy::sanitize`, shared `build_relay_request`; collapsed the duplicate server↔relay wire DTOs; renamed `RemotePushRoute -> PushRoute` (+`ApnsDirect`) through `state.rs`, admin API/HTML, tests; added `NOD_APNS_DIRECT_*` config; updated `ARCHITECTURE_NOTES.md` §3 to IMPLEMENTED.
- #9 UniFFI: created `nod-proto-ffi`; `build-nod-proto-ffi.sh`; `Package.swift` binaryTarget + `NodProtoFFI` target (Swift 5 mode); pointed `NodSigningKeyStore` at `NodProtoFFI.decisionSigningPayload`; fixed Swift-6 concurrency + iOS-sim x86_64 link.
- Verified #10 on Swift: `swift build`, `xcodebuild` NodIOS, `swift test`; deleted the ghost `testLegacyServerContractKeysDoNotDecode`; fixed `README.md` `Channels/NodKit` → `Sources/NodKit`.
- #8 typeshare: annotated `nod-proto` + `nod-client-core`; `scripts/generate-types.sh`; found + fixed the desktop `source→channel` frontend bug (66 hits / 9 files + file rename).
- Created `.project/` tracker (this folder).

## Evidence

```text
# Rust workspace
cargo test --workspace  -> ok suites: 20  (0 failures)

# Swift
swift test              -> Executed 24 tests, 0 failures
xcodebuild -scheme NodIOS -sdk iphonesimulator CODE_SIGNING_ALLOWED=NO  -> ** BUILD SUCCEEDED **
xcodebuild -scheme NodMac CODE_SIGNING_ALLOWED=NO                       -> ** BUILD SUCCEEDED **
xcodebuild -version     -> Xcode 26.5 ; swift 6.3.2

# Desktop frontend — CURRENT tree (mid full-adoption, red):
npm run typecheck ->
  src/domain.test.ts(131,67): TS2322 '"resolved"' not assignable to 'RequestStatus'
  src/domain.test.ts(132,65): TS2322 '"pending"' ...
  src/domain.test.ts(186,48): TS2322 '"approve_with_text"' not assignable to 'OptionKind'
  src/domain.ts(20,3):  TS2322 '"dismiss"' not assignable to 'OptionKind'
  src/domain.ts(94,3):  TS2322 'string | undefined' not assignable to 'string'
# (Earlier in-session, with a hand-written models.ts, tsc was clean + 16 vitest green.)

# typeshare limitation hit:
typeshare ... -> Parsing error: Unsupported type: "usize", models.rs:134  (fixed via serialized_as = "HashMap<String, u32>")

# Git
git log -1 --oneline -> b6765f4 Rework
git status --short    -> 112 M / 3 RM / 3 ?? (118 total)
grep skip_serializing_if nod-proto/src -> (none)  # wire-omit-null change NOT in this tree
```

## Results

- #9, #10, #11 complete and green (Rust + Swift).
- #8 infra in place + desktop naming bug fixed; literal TS adoption is half-applied in this tree (models.ts re-exports generated; `domain.*` + wire change pending in the fork) → desktop `tsc` red here.
- `.project/` tracker created.

## Learnings

- Xcode/Swift available locally (earlier "can't run Xcode" claim was wrong).
- typeshare 1.13: no camelCase Swift; TS `enum`s (not unions); `Option<T>` → `?: T` (not `| null`); no `usize`.
- Desktop TS consumes `nod-client-core` view types over Tauri IPC; it had drifted to `source` while the backend emits `channel`.
- The relay was already a lib; the duplicate wire DTO was the real drift risk.

## Artifacts And Paths

- `nod-proto-ffi/`, `scripts/generate-types.sh`, `client/nod-apple/scripts/build-nod-proto-ffi.sh`, `client/nod-desktop/src/dto/{generated.ts,models.ts}`, `.project/`.

## Decisions

- Dedicated `nod-proto-ffi` crate; xcframework built-from-source (gitignored); Swift 5 mode for the shim; relay route by config presence (mutually exclusive); #8 TS-only typeshare (Swift hand-written). See `decisions.md`.

## Questions And Hypotheses

- Open question: adopt `skip_serializing_if` (omit nulls) globally to unblock literal TS adoption? (fork testing)
- Hypothesis: full literal adoption is more maintainable with less drift than the hand-written mirror — being validated in the fork.

## Next Steps

- Reconcile with the #8 fork; finish/merge the wire change + enum call-site fixes to get desktop green; then decide commit strategy for the 118-file milestone. See `next-actions.md`.

## Unknowns

- Exact diff/outcome of the parallel #8 fork (not visible from this tree beyond the partial snapshot: `models.ts` re-export, TS-only `generate-types.sh`, un-ignored `generated.ts`).
- Whether the user wants the milestone committed before or after the fork merges.
