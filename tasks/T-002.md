---
id: T-002
title: Board setup + baseline green gate
status: done
assignee: Claude
priority: null
tags: [release]
depends_on: []
created_at: 2026-06-10
updated_at: 2026-06-10
---

## Goal

Clean, verified-green baseline before any release work: ordna board populated with the release plan, `.project/` removed from version control (personal workspace tracking stays local), pending nod-apple version-stamping work committed, and the full test gate re-verified (5 commits have landed since the last green run on 2026-06-09).

## Acceptance Criteria

- [x] Board has one card per release phase (T-002..T-013) with goals/AC/deps; web UI reachable
- [x] `.project/` untracked via `git rm -r --cached` (files stay on disk); duplicate `/.project` gitignore lines deduped
- [x] nod-apple version-stamping changes committed (`scripts/build-macos-app`, `Apps/NodMac/Resources/Info.plist`, `README.md`, `docs/APPLE.md`)
- [x] `cargo test --workspace` green (~150 tests / 20 suites)
- [x] `cd client/nod-apple && swift test` green (22 tests); `xcodebuild` NodMac + NodIOS build with `CODE_SIGNING_ALLOWED=NO`
- [x] `cd client/nod-desktop && npm run typecheck && npm test` green (16 vitest)
- [x] `cargo fmt --check` and `cargo clippy --all-targets -- -D warnings` clean

## Notes

User decision: `.project/` is personal tracking — keep private to the workspace, never commit. Optional follow-up (user's call, not done unasked): purge old `.project/` revisions from public history with `git filter-repo` + force-push.

Discovered during execution: `AGENTS.md`, all component `PROJECT.md` files, `ARCHITECTURE_NOTES.md`, and `ext-docs/` are already untracked + gitignored (local working docs). Only `.project/` was the tracked-but-ignored anomaly. Public docs = root README, component READMEs, `server/nod-server/docs/API.md` — the public README must not link local-only files.

## Progress

2026-06-10: Board stood up (T-002..T-013, review/blocked columns added to config). `.project/` untracked in 50adc31; nod-apple stamping committed in e4ed2ff; board committed in 5ba406e. Green gate run: cargo test --workspace exit 0, swift 22/22, NodMac + NodIOS BUILD SUCCEEDED (CODE_SIGNING_ALLOWED=NO), tsc clean, vitest 16/16, clippy -D warnings clean. cargo fmt --check found drift in 17 files (nod-client-core, nod-tui, ffi, proto) — applied and committed. No review pass needed: administrative task, no logic changes (fmt is whitespace-only).
