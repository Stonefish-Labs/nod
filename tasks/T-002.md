---
id: T-002
title: Board setup + baseline green gate
status: doing
assignee: Claude
priority: null
tags: [release]
depends_on: []
created_at: 2026-06-10
updated_at: 2026-06-10
---

## Goal

Clean, verified-green baseline before any release work: ordna board populated with the release plan, `.project/` removed from version control (personal workspace tracking stays local), pending nod-apple version-stamping work committed, and the full test gate re-verified (5 commits have landed since the last green run on 2026-06-09).

## Acceptance Criteria

- [ ] Board has one card per release phase (T-002..T-013) with goals/AC/deps; web UI reachable
- [ ] `.project/` untracked via `git rm -r --cached` (files stay on disk); duplicate `/.project` gitignore lines deduped
- [ ] nod-apple version-stamping changes committed (`scripts/build-macos-app`, `Apps/NodMac/Resources/Info.plist`, `README.md`, `docs/APPLE.md`)
- [ ] `cargo test --workspace` green (~150 tests / 20 suites)
- [ ] `cd client/nod-apple && swift test` green (22 tests); `xcodebuild` NodMac + NodIOS build with `CODE_SIGNING_ALLOWED=NO`
- [ ] `cd client/nod-desktop && npm run typecheck && npm test` green (16 vitest)
- [ ] `cargo fmt --check` and `cargo clippy --all-targets -- -D warnings` clean

## Notes

User decision: `.project/` is personal tracking — keep private to the workspace, never commit. Optional follow-up (user's call, not done unasked): purge old `.project/` revisions from public history with `git filter-repo` + force-push.

## Progress
