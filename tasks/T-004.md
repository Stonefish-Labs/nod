---
id: T-004
title: TUI smoke test + manual E2E checklist
status: done
assignee: Claude
priority: null
tags:
  - release
  - tui
depends_on:
  - T-002
created_at: 2026-06-10
updated_at: 2026-06-10
---

## Goal

TUI verified as release-ready: the PROJECT.md event-loop smoke test exists (enroll → render → submit → state update), and a manual E2E checklist has been executed once against a local server.

## Acceptance Criteria

- [ ] Event-loop smoke test in `client/nod-tui` using `FakeRuntime` (promoted into `src/test_support.rs`) + `ratatui::backend::TestBackend`: enroll flow emits the runtime command, render shows the pending request title, option submit calls `submit_option` and the resolved state re-renders
- [ ] In-flight command gate drop-rest behavior and cached-state merge (`apply_request`/`apply_device`) pinned by test or documented per PROJECT.md
- [ ] `docs/release-checklist.md` created with a TUI section: enroll via admin code (keyring) + insecure-store variant, live request via `examples/issuer.py`, approve-with-text, reject, server-restart WS reconnect, TUI-restart profile persistence, Terminal.app/iTerm2/resize
- [ ] Checklist executed once locally, dated in the file
- [ ] `cargo test -p nod-tui` green; `client/nod-tui/PROJECT.md` updated

## Notes

TUI is a thin renderer over `nod-client-core` (`RuntimePort` is the only seam). `NOD_CLIENT_CORE_INSECURE_TOKEN_STORE=1` enables keyring-free testing.

## Progress

2026-06-10: Done in 8e3abfe + review follow-up. Smoke test (enroll → render → submit → state update) via FakeRuntime + TestBackend; FakeRuntime promoted to test_support; all PROJECT.md doc comments added. Live tmux E2E against a local server (sandboxed NOD_CLIENT_CORE_STATE_DIR + insecure store): enroll, live WS request, signed approve/reject/approve-with-text (all verified:true server-side), server-restart reconnect, profile persistence across relaunch, 60x20 resize. BUG FOUND + FIXED during E2E: detail pane hints a/r for *_with_text options but the key lookup matched exact kinds only — option_for_kind now falls back to the with-text variant (regression test). docs/release-checklist.md created (TUI section executed + dated; forward references tagged with their board tasks). Review: NEEDS CHANGES on checklist honesty → fixed (T-NNN tags, subshell green gate, resolved-render assertion, "command seam" wording). Left manual deliberately: keyring path (must not write real macOS keychain from automation) and the subjective terminal visual pass — both listed in the checklist.