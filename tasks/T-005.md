---
id: T-005
title: Desktop drift check + small items
status: done
assignee: Claude
priority: null
tags:
  - release
  - desktop
depends_on:
  - T-002
created_at: 2026-06-10
updated_at: 2026-06-10
---

## Goal

Desktop (Tauri) client cleared of its PROJECT.md pre-release items: the typeshare drift-diff becomes a runnable check with the known drift adjudicated, and a logic-level dev-mode pass on macOS happens before Windows VM time is spent.

## Acceptance Criteria

- [ ] Drift adjudicated: regenerate `src/dto/generated.ts` (`scripts/generate-types.sh`, needs typeshare-cli), decide `Decision.signature` omission (likely intentional — UI never renders it) and resolve the `notification_delivery_mode` discrepancy
- [ ] `scripts/check-drift.mjs` (dependency-free Node) compares interface fields with an inline documented `INTENTIONAL_OMISSIONS` allowlist; exits non-zero on unexplained drift; wired as `npm run drift-check`
- [ ] `Request`→`NodRequest` rename commented in `models.ts`
- [ ] Desktop README gets macOS scope note (dev on macOS fine; shipped platform = Windows; macOS users use the native app)
- [ ] Vitest pins optimistic-splice behavior in `submitRequestOption`
- [ ] `npm run tauri dev` pass on macOS against a local server: enroll, receive request, decide, tray behavior
- [ ] `npm run drift-check`, `npm run typecheck`, `npm test` all green

## Notes

`generated.ts` is git-ignored by design — hand-written `models.ts` is the contract, typeshare output is drift-reference only (decisions.md). Don't import generated types.

## Progress
