---
id: T-006
title: Apple vestige cleanup
status: done
assignee: Claude
priority: null
tags:
  - release
  - apple
depends_on:
  - T-002
created_at: 2026-06-10
updated_at: 2026-06-10
---

## Goal

Pre-migration Swift vestiges deleted before release (burn the boats): the dead types/methods only their own tests reference, and the duplicated keychain-account literal.

## Acceptance Criteria

- [ ] `NodSyncEnvelope` deleted along with its tests
- [ ] `NodSigningKeyStore.sign(_:)` deleted along with its tests (keep the rest of the type if still used)
- [ ] `decisionSigningKey.<id>` keychain-account literal deduped into a single constant shared by NodStore and SecureEnclaveDeviceSigner
- [ ] `swift test` green (count drops by the deleted tests); `xcodebuild` NodMac + NodIOS build
- [ ] macOS app launches and operates (quick sanity)
- [ ] `client/nod-apple/PROJECT.md` items ticked

## Notes

Deliberately NOT in scope (deferred post-release): collapsing NodStore's mirrored `@Published` surface onto `NodRuntimeState` — that's an architectural refactor of working code, not a vestige; it would re-open verification of the operable macOS/iOS apps.

## Progress

2026-06-10: Done in 45b7681 + dead-extension follow-up. Deleted: NodSigningKeyStore.sign(_:) with its private payload/timestamp helpers and the missingDeviceIdentity/missingRequestDigest error cases; NodDecisionSignature, NodDecisionSigningRequest, NodSyncEnvelope; their tests; the orphaned nilIfEmpty extension (review catch). Keychain literal single-sourced as NodSigningKeyStore.account(for:) — byte-identical string, both call sites delegate, no key orphaning. Suite 22 -> 20 green (exactly the vestige tests); NodMac + NodIOS BUILD SUCCEEDED. Reviewer confirmed zero lingering references repo-wide and that surviving tests keep the meaningful coverage (provision -> restore -> signPayload verified against Rust). Full app launch sanity happens from the stapled DMG in T-011's checklist run.
