---
id: T-008
title: Version unification 1.0.0
status: todo
assignee: null
priority: null
tags: [release]
depends_on: [T-002]
created_at: 2026-06-10
updated_at: 2026-06-10
---

## Goal

One version everywhere: 1.0.0 across the Cargo workspace, Tauri config, and npm manifest, with the Apple apps on their existing 1.0.x train. Repository metadata corrected.

## Acceptance Criteria

- [ ] `[workspace.package] version = "1.0.0"` in root Cargo.toml; all member crates on `version.workspace = true` (nod-proto, nod-client-ffi, nod-server, nod-apns-relay, nod-client-core, nod-tui, desktop src-tauri)
- [ ] Straggler `edition`/`license` fields normalized to `.workspace = true`
- [ ] `repository` fixed to `https://github.com/batteryshark/nod` (currently says Stonefish-Labs)
- [ ] `tauri.conf.json` and desktop `package.json` at 1.0.0
- [ ] Release docs/scripts invoke macOS builds with `NOD_MAC_MARKETING_VERSION=1.0.0`
- [ ] `Cargo.lock` refreshed; `grep -rn '0\.1\.0' --include='*.toml' --include='*.json'` clean; workspace tests still green

## Notes

User decision: 1.0.0 (not 0.1.0) because App Store Connect already has 1.0 iOS builds and cannot downgrade — unifying up is the only consistent option. Tag will be annotated `v1.0.0` (cut in T-012).

## Progress
