---
id: T-009
title: Deploy docs + docs overhaul + AGPL license
status: todo
assignee: null
priority: null
tags: [release, docs]
depends_on: [T-003, T-007, T-008]
created_at: 2026-06-10
updated_at: 2026-06-10
---

## Goal

A stranger with a MacBook/Windows/Linux box can run Nod in under five minutes without Docker, the repo reads as a deliberate human-written project, and the license is AGPL-3.0.

## Acceptance Criteria

- [ ] Relicense MIT → AGPL-3.0: single root `LICENSE` (full AGPL-3.0 text, copyright Stonefish Systems); delete stale MIT `client/LICENSE` + `server/nod-server/LICENSE`; `license = "AGPL-3.0-only"` in `[workspace.package]` with crates on `license.workspace = true`; desktop `package.json` license field updated
- [ ] `docs/deploy.md`: Path A prebuilt binary per-OS (download → `NOD_ADMIN_TOKEN=$(openssl rand -hex 24) ./nod-server` → `http://localhost:8767/admin`; PowerShell variant + SmartScreen note; optional launchd/systemd/Task Scheduler snippets), Path B Docker (`ghcr.io/batteryshark/nod-server` one-liner + compose pointer), Tailscale Serve + reverse-proxy note, APNs as advanced pointer ("only needed for iPhone/iPad push")
- [ ] Root README rewritten for public consumption: interrupt/decision-surface framing, why-not-chat paragraph, 5-minute quickstart, clients matrix, screenshots, architecture pointer, AGPL badge + one-line self-host note
- [ ] Per-component README accuracy pass (server, client/, TUI, desktop, Apple): every command copy-paste-tested, no filler words, consistent naming, `grep -rn "Stonefish-Labs"` returns nothing
- [ ] Trackers reconciled: AGENTS.md boxes ticked, resolved PROJECT.md items deleted (not annotated)
- [ ] deploy.md Path A executed literally on this Mac from a CI-dispatch artifact in a fresh temp dir

## Notes

User decisions: AGPL-3.0 chosen over MIT/Apache (network copyleft — blocks closed SaaS forks; sole author so relicense is clean); deployment docs must serve ordinary machines, not just servers — prebuilt binary is the headline path, Docker secondary. Depends on T-003 (nod-smoke + embedded admin.html are documented), T-007 (GHCR image + artifacts exist), T-008 (version in docs).

## Progress
