---
id: T-010
title: Windows E2E in VM
status: blocked
assignee: null
priority: null
tags:
  - release
  - desktop
  - windows
depends_on:
  - T-005
  - T-007
created_at: 2026-06-10
updated_at: 2026-06-10
---

## Goal

The Windows client verified end-to-end using the actual CI-built MSI in a Windows VM on the Mac, with SmartScreen behavior documented and the tray icon visually confirmed for the first time.

## Acceptance Criteria

- [ ] MSI sourced from a release.yml `workflow_dispatch` run (bits-identical to the release pipeline)
- [ ] VM reaches host server (`http://<host-ip>:8767/health` from VM browser; Parallels/VMware shared net → host LAN IP, UTM → 10.0.2.2)
- [ ] SmartScreen "More info → Run anyway" flow screenshotted (feeds deploy.md + release notes); `Get-FileHash` matches published SHA-256
- [ ] Windows tray nod-head icon visually verified — light/dark taskbar, tooltip, Show/Quit menu (first time seen; macOS half already user-verified)
- [ ] Enroll → receive request (issuer.py from host) → Windows toast → approve-with-text → reject → verified on server
- [ ] Persistence: app relaunch keeps enrollment (Credential Manager); VM reboot keeps enrollment; autostart behavior checked
- [ ] WS reconnect on server restart; uninstall removes tray; reinstall works
- [ ] Windows section of `docs/release-checklist.md` complete; screenshots in `docs/images/`

## Notes

User decision: test in a Windows VM (Parallels/VMware/UTM) on this Mac. Windows MSI ships UNSIGNED in v1 — SmartScreen documentation is a deliverable, not a bug. If the tray icon art needs changes: swap `src-tauri/icons/tray-nod.png` + rebuild.

## Progress

2026-06-10: Staged and ready for your VM session — everything below is prepared; the session itself is yours to run.

- MSI (CI-built, dispatch run 27263859330, includes the Windows toast fix that had never compiled before): ~/Desktop/nod-release-staging/Nod_1.0.0_x64_en-US.msi
  SHA-256: 1c025e423e6b88aeec660a63eb250a65cd8da1dc61e9c0c1da0e57d0dca20b30 (also in the .sha256 file next to it)
- Host prep (run on this Mac):
    cd ~ && mkdir -p nod-vm-test && cd nod-vm-test
    NOD_BIND=0.0.0.0:8767 NOD_ADMIN_TOKEN=<pick-one> /path/to/nod-server   # or: cargo run -p nod-server
    Admin panel from the VM browser: http://192.168.1.53:8767/admin (Parallels/VMware shared net; UTM uses http://10.0.2.2:8767)
- Checklist: docs/release-checklist.md "Windows desktop" section (SmartScreen screenshots feed the release notes; first visual check of the tray nod-head icon).
- Note for re-runs: if code changes, re-dispatch the Release workflow (gh workflow run release.yml --ref main) and grab the fresh windows-msi artifact.

Marking blocked = waiting on the user-driven VM session.
