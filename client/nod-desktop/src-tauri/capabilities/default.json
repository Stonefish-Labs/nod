{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Main window baseline: core APIs incl. event.listen for runtime messages. Custom commands are not ACL-gated; plugins are Rust-side only.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
